{
  "name": "adl-website",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/gen-og-images.mjs && astro build",
    "check": "tsc --noEmit && node scripts/validate-site.mjs",
    "validate": "npm run build && npm run check",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "astro-pagefind": "^2.0.1",
    "pagefind": "^1.5.2",
    "sharp": "^0.34.2",
    "typescript": "^5.9.3"
  }
}
